# Project Memory

## Decisions

- 2026-07-05: Windows build is driven from WSL via `scripts/build-windows.sh`. Key choices:
- 2026-07-05: **VS generator (MSBuild), NOT Ninja.** Ninja's stat-based dependency model breaks on the `\\wsl.localhost`/9P mount (CMake try-compile source shows up as "missing"); MSBuild tolerates the mapped drive. Generator auto-picked from the installed VS major version (16=2019, 17=2022; `MCU_VS_GENERATOR` overrides).
- 2026-07-05: **`pushd \\wsl.localhost\<distro>\...`** maps the repo UNC to a temp drive (Z:), so MSVC/rc.exe/juceaide read source straight off the WSL filesystem — no copy to /mnt/c.
- 2026-07-05: **Portable CMake 3.31.6** cached under `~/.cache/csurf-klinke-mcu/` (JUCE 8 needs >=3.22; VS2019 bundles 3.20).
- 2026-07-05: **`CMAKE_SUPPRESS_REGENERATION=ON`** silences MSB8064/65 regen-dependency warnings (unreliable over 9P); script always re-runs configure, so no cost.
- 2026-07-05: **build dir is `build_win/`** (multi-config VS generator → `build_win/Release|Debug/`), kept separate from the Linux `build/`. `.gitignore`d.
- 2026-07-05: `scripts/build-windows.sh` incremental model: the default (no flags) SKIPS the CMake configure step and only builds — configure runs once initially, or on `--clean`/`--reconfigure`. Rationale: configure is ~40s (re-runs the JUCE/juceide setup) and bumps the `VERSION.txt` build counter, so running it every build was wasteful and inconsistent with the Linux `cmake ..`-once / `cmake --build`-repeatedly model. `--reconfigure` is required after editing `CMakeLists.txt` or the source list (CMAKE_SUPPRESS_REGENERATION=ON means MSBuild will not auto-reconfigure).
- 2026-07-06: Directory restructure plan v3 written to `docs/directory-restructure-plan3.md` (supersedes v1/v2). v2's blockers are all gone (Windows CMake works on all 3 platforms; `.vcxproj` dead since 2024-07-14; no upstream remote). Plan is viable now, not yet executed.
- 2026-07-06: Restructure design decisions: (a) `util/` merged into `core/` — no 2-file folder; McuAssert.h + std_helper.h go to src/core/. (b) MeterBridge subclasses move to their modes (each couples to its mode); only abstract base stays in display/. (c) `editor/` subfolder for EVERY mode with `*Component` files, via objective rule "name ends in Component". (d) Include strategy = `file(GLOB_RECURSE ... CONFIGURE_DEPENDS)` on src/ → 0 include rewrites (not 437). (e) Dead VS files → archive/vs-legacy/.
- 2026-07-06: `ActionsDisplay.{cpp,h}` → `modes/commands/` (NOT display/). OVERRIDE of the structural-coupling rule on maintainer's domain authority: the labels it renders ARE the Action-Mode actions (m_strLabel[16][8], GlobalActions.xml), shown via the global Name/Value button (OnNameValue, ButtonManager.cpp:91 CC 0x34). Compile-time it couples to Display/DisplayHandler/csurf_mcu and is owned as a CSurf_MCU member, but behavioral cohesion wins. Pre-existing smell: core then includes a modes/commands/ header (core→mode inversion); ownership transfer to CommandMode is a separate future refactor. Do NOT re-litigate this on include-graph grounds — see docs/directory-restructure-plan3.md §3d.
- 2026-07-06: Restructure scope expanded (maintainer call): NOTHING project-owned stays in repo root except build-system files (CMakeLists.txt, Version.h.in, VERSION.txt) and top-level docs/license (AGENTS.md, gplv3.txt, readme.txt, *.md, *.org). Concretely: (a) ALL scripts incl. fetch_deps.sh → scripts/ (fetch_deps.sh + build_and_run.sh need a `dirname/..` repo-root fix on move; debug_reaper.sh + start_reaper.sh are path-independent, zero change); (b) headers out of root — csurf.h + reaper_plugin_functions.h → vendor/, JuceHeader.h → src/, mcu_button_defines.h → src/core/; (c) resources (res.rc, resource.h, res.rc_mac_dlg) → resources/ (sibling of src/). vendor/ + resources/ become extra target_include_directories entries (csurf.h #includes resource.h; res_linux.cpp #includes res.rc_mac_dlg — both need the path).
- 2026-07-06: vendor/ + resources/ are TOP-LEVEL SIBLINGS of src/ (symmetric with fetched deps juce_8/, reaper-sdk/, boost_1_91_0/), NOT under src/. Keeps vendored/resources out of "source". Cost: 2 explicit include-path lines beside the src/ GLOB.
- 2026-07-06: Extender-support project started (planning phase, results → ai-docs/). Goal: combine multiple MCU units into one surface. Hardware target: up to 8 units (64 ch); test setup: 1 main + 2 extenders (24 ch). References: Cockos original csurf_mcu.cpp (per-unit CSurf_MCU + static m_mcu_list), CSI, DrivenByMoss (one logical surface, N×8 ch).
- 2026-07-06: Extender support was deliberately REMOVED in v0.8. Evidence: csurf_mcu.cpp parseParms() forces offset=0/banksize=8 with comment "since v0.8 we don't support extender"; createFunc() blocks a 2nd instance ("only a single instance can be used"); csurf_main.cpp:395 Register(&csurf_mcuex_modified_reg) commented out (extern at :11 too); csurf_mcu.h EXT_ID="unused" (was the MCUEX type-string test). Dead scaffolding remains: g_mcu_list, m_is_mcuex, m_offset, IsExtender(), csurf_mcuex_modified_reg struct, Tracks::adjust(g_mcu_list.GetSize()*8), DisplayHandler::EnumMCUType{MCU,MCU_EX,PROX}.
- 2026-07-06: ARCHITECTURE DECISION = Option B (one logical surface owns N hardware units). User adds ONE Reaper surface; config-dialog lists units (main + up to 7 extenders) each with its own MIDI in/out. CSurf_MCU is a single instance owning HardwareUnit[0..N] driving N*8 channels. Chosen over Option A (Cockos-style per-unit Reaper surfaces via g_mcu_list) because it dissolves the Tracks-single-m_pMCU problem cleanly.
- 2026-07-06: MODE COUPLING DECISION = linked first (all units follow the main unit's active mode). BUT architecture must stay open for per-mode-independent operation later (maintainer flagged future "pro mode"). Do not hard-couple in a way that rules out independent modes.
- 2026-07-06: ROADMAP DECISION = build the SCAFFOLDING first (no mode work yet): HardwareUnit abstraction, config-dialog + configstring, Tracks singleton refactor to N*8, offset/bank logic, global-display ownership. Per-mode design (multitrack/plugin/commands/sends) comes AFTER the scaffolding is in place.
- 2026-07-06: Channel model decisions (extender): (a) Offset field DROPPED — unit position is implicit = unitIndex*8 (no manual offset in config dialog; matches Option B). (b) Bank scroll = the WHOLE N*8 bank scrolls together via globalOffset (Channel Up/Down + track-selection-follow move the entire bank); matches Cockos original + linked-modes default. (c) Master fader = PER UNIT, not main-only — semantics of what each unit's master fader controls still OPEN (asked maintainer).
- 2026-07-06: Working assumption for channel indexing: flat strip index 1..N*8 across all units (so mode loops `channel=1..N*8` generalize trivially), master handled as a separate per-unit concept. Revisit if master semantics force otherwise.
- 2026-07-06: Master-slot semantics RESOLVED (extender): the 9th fader slot per unit is NOT architecturally special — the active MODE decides what each unit's master slot controls (Reaper master, dry/wet, folder master, …). In linked mode all N master slots coincide; in a future independent-mode world they can differ per unit. Consequence: the current "channel 0 = Reaper master" special-casing (getMediaTrackForChannel(0), OnFaderMove tid==8, FIXID id==8→-1) must move OUT of the core into mode logic (MultiTrackMode assigns unit 0's master slot → Reaper master to preserve current behaviour).
- 2026-07-06: Per-unit hardware reality (extender): each MCU unit = 8 FULL strips (fader + VPOT + select/mute/solo/rec + 2-line display) + 1 FADER-ONLY master slot (fader + touch only; NO VPOT/select/solo/mute/rec/display). So the master slot is constrained to fader-style control. Strips addressed flat 1..N*8; master slot is a separate per-unit concept.
- 2026-07-06: CORRECTION to earlier master-slot note: the master fader (channel 0) is ALREADY mode-defined today (MultiTrackMode→Reaper master, PlugMode→dry/wet via fader(channel==0), SendReceive→selected-track vol). FIXID macro in csurf_mcu.h is DEAD CODE (never expanded anywhere). OnFaderMove's tid==8→channel 0 is just physical→logical fader mapping (9th fader = master slot), not a Reaper-master binding. The only core-level master ref is Tracks::m_channelTracks[0]=CSurf_TrackFromID(0) (mixer default, benign). So NO 'move master special-casing out of core' refactor is needed — just expose one master slot per unit; modes decide meaning.
- 2026-07-06: Mackie SysEx device IDs confirmed (NicoG60/TouchMCU protocol doc): manufacturer 00 00 66; 0x14 = Mackie Control Universal/Pro (MAIN), 0x15 = MCU XT (EXTENDER), 0x10/0x11 = Logic Control/XT, 0x05 = HUI. Current code addHeader() already switches 0x14 (MCU) vs 0x15 (MCU_EX); PROX second-display uses non-standard 0x67 0x15. LCD cmd 0x12 = one 2x56 center LCD per unit (offset 0x00-0x37 upper line, 0x38-0x6F lower). Per-unit display addressing => DisplayHandler must be PER UNIT (own MIDI port + own device id), not one global.
- 2026-07-06: Per-unit config model (proposed, points 3+4 from maintainer): each unit = (MIDI in, MIDI out, EnumMCUType {MCU=main 0x14, MCU_EX=extender 0x15, PROX=QCon ProX main w/ 2nd 2x56 LCD}). This subsumes the existing GLOBAL CONFIG_FLAG_PROX (becomes per-unit PROX type). Main unit (MCU/PROX) owns transport + 7-seg timecode/assignment; extender (MCU_EX) = channel strips + its own center LCD only. EXACT display hardware of ProX vs extenders still TBD (asked maintainer).
- 2026-07-06: GENERALITY DECISION (extender config): the extension will be PUBLISHED, so per-unit config must support EVERY hardware combination — NOT a fixed EnumMCUType set. Each unit is described by INDEPENDENT capabilities: (MIDI in, MIDI out, SysEx device ID {0x14,0x15,...}, center-LCD panels {0,1,2}, has-transport+7seg {bool}). EnumMCUType {MCU,MCU_EX,PROX} survives only as dialog CONVENIENCE PRESETS that pre-fill the capabilities; a "custom" mode exposes them individually. (#LCD-panels=2 implies the ProX 2nd-panel path 0x67 0x15.)
- 2026-07-06: ORTHOGONALITY RULE (extender) — config position vs. global routing are INDEPENDENT axes: (a) config position of a unit = its CHANNEL-STRIP ORDER only (pos0→ch1-8, pos1→ch9-16,...), mirrors physical left→right; (b) GLOBAL routing (transport LEDs, SMPTE/7-seg timecode, assignment, automode LEDs, flip/globalview, drop, save/undo, metronome) goes to EVERY unit whose capabilities include transport+7seg, REGARDLESS of position. NO "topmost unit is the main" rule. A transport-capable unit can sit at any position; need ≥1 such unit. (Corrects an earlier wrong statement that Unit 0 = main.)
- 2026-07-06: PROTOCOL SCOPE narrowed (extender): HUI (0x05) and Logic Control (0x10/0x11) are OUT (HUI=different protocol; Logic Control never supported, never will). Only MCU-family device ids remain: 0x14 (main) / 0x15 (extender). Consequence: device id is NO LONGER an independent capability — it is DERIVED from the main/extender role. LCD panels ∈ {1,2} only (no displayless unit). Capability model collapses to TWO independent knobs: {isMain bool, lcdPanels 1|2}.
- 2026-07-06: DIALOG PRESETS = 4 named options forming the 2x2 grid {main,extender} x {1 LCD, 2 LCD}: Mackie Main / Mackie Extender / QCon ProX / QCon ProX extender. Internally store the 2 capabilities (preset = convenience). OPEN: confirm QCon ProX extender = 2 LCDs (clean grid) or 1 LCD (=duplicate of Mackie Extender); and verify the 0x67 2nd-LCD path applies to a 2-LCD extender too (hardware check at WP-A).
- 2026-07-06: QCon ProX extender = 2 LCDs CONFIRMED (maintainer). The 4 presets form a clean 2x2 grid {main,extender} x {1 LCD, 2 LCD}; all 4 are distinct. The 0x67 2nd-LCD path applies to any 2-LCD unit (main or extender). Per-unit capability model is now FULLY RESOLVED: (MIDI in, MIDI out, isMain, lcdPanels in {1,2}); device id derived (main=0x14, ext=0x15).
- 2026-07-06: Boost upgraded 1.39.0 → 1.91.0 (latest stable, header-only). Rationale: Boost 1.39 (2009) does NOT compile under modern libc++/C++17 without an ever-growing pile of patches (shared_ptr move-ctor, expired_slot incomplete type, std::auto_ptr/unary_function/binary_function removal, assert hijack). 1.91.0 compiles clean on all platforms. Pinned in fetch_deps.sh, fetched as boost_1_91_0/.
- 2026-07-06: Portable Linux distribution strategy = build in a Debian 11 (bullseye) container (glibc 2.30 floor) with STATICALLY-LINKED C++ runtime (-static-libstdc++ -static-libgcc). Drops GLIBC requirement from 2.43→2.30 and removes libstdc++/libgcc_s as runtime deps. Result: runs on Ubuntu 20.04+, Debian 11+, Fedora 31+, Arch, openSUSE Leap 15.4+, RHEL/Rocky/Alma 9+. Files: docker/release-linux.Dockerfile, scripts/build-portable-linux.sh, .dockerignore.
- 2026-07-06: Windows CMake = x64 only (no Win32). EXT_B not built in initial CMake branch (will be re-added later as part of extender support). Output follows .vcxproj convention: reaper_csurf_mcu_klinke_x64.dll.
- 2026-07-06: JUCE 1.52 amalgamated → JUCE 8 modules. C++14 → C++17 (required by JUCE 8). Libcurl added as linux system dep (JUCE 8 needs it). KlinkeLookAndFeel.h provides per-window LookAndFeel_V4 subclass for colour fixes on JUCE 8 dialogs.
- 2026-07-06: DEVICE-MODEL-PRIMARY decision (extender config): the UnitConfig stores the DEVICE MODEL (enum DeviceModel {Mackie, QConProX}), NOT lcdPanels. Reason: "QCon ProX" is a device variant with a CLUSTER of protocol quirks, of which "2 LCD panels" is only ONE — the others (VPOT-ring byte3=0, meter-bridge master path in MeterBridge + SendReceiveMeterBridge, LED-blink/EmulateBlinkingLEDs handling, assignment-display suppression in setAssignmentDisplay) are NOT lcd-related. So lcdPanels is DERIVED (QConProX→2, Mackie→1), and all ~20 CONFIG_FLAG_PROX checks become per-unit unit.isProX() (model==QConProX). The 4 presets are {Main,Extender}×{Mackie,QConProX}. Rejects the alternative "lcdPanels==2 means ProX" as a leaky proxy. Final UnitConfig = (midiInDev, midiOutDev, isMain, model).
- 2026-07-06: ProX capability inventory (the ~20 CONFIG_FLAG_PROX sites): Display-related (2nd panel rows 2/3 + 0x67 0x15 path) in DisplayHandler + MultiTrack/Command/Pan/Plug/Send(updateDisplayProX); NON-display quirks: VPOT_LED byte3=0, MeterBridge::updateMasterLEDs + SendReceiveMeterBridge::updateMasterLEDs separate path, SetLED LED_BLINK_BYPASSED + EmulateBlinkingLEDs, setAssignmentDisplay skips CC 0x4A/0x4B on ProX. All become per-unit isProX().
- 2026-07-06: ANCHOR/QUICK-JUMP GENERALISATION (extender): anchors and quick-jump slots generalise from 1..8 to 1..N*8 (as many as total available channels). The anchor free-slot calc `8 - numAnchors` (Tracks.cpp:448) becomes `availableChannels() - numActiveAnchors()`. This CONFIRMS the persisted `anchor`/`q_channel` values are absolute surface slots 1..N*8 (so out-of-range handling is real, not just hypothetical) — earlier corrected-back-and-forth; now settled.
- 2026-07-06: NEW REQUIREMENT "release the extenders" (dynamic unit activation). A fixed MCU button on the main unit toggles a mode where the extension reacts to / sends to ONLY the main unit, ignoring extenders (so the user can drive another app with the extender hardware). Decisions: (a) trigger = FIXED MCU BUTTON (which one TBD, must be conflict-free) — NOT a Reaper action; (b) scope = BINARY toggle (main-only vs main+all-extenders); (c) persistence = EPHEMERAL/session (default on load = all active); (d) MIDI ports = KEEP OPEN (multi-client) + suppress I/O on inactive units (do not close/reopen). Architectural impact: availableChannels() becomes a RUNTIME quantity (sum of active units' strips); HardwareUnit gains an `active` flag (inactive ⇒ ignore input + suppress output); numActiveAnchors() counts only anchors within the active range (so released extenders' anchors don't count); toggle triggers a reflow (adjust). Cross-cutting: touches WP-A (active flag), WP-C (availableChannels runtime), WP-D (bank size runtime).
- 2026-07-06: PERSISTENCE (extender, settled): two levels — (a) surface configString is Reaper-GLOBAL (not project), format changes 5-int → N×(in,out,isMain,model)+flags, legacy 5-int parses as 1 Mackie-main unit (WP-B); (b) project state is the <MCU_KLINKE> XML chunk in the .rpp, GUID-keyed per track + global Options/PlugMaps/CommandMode/Actions. Only anchor/q_channel are unit-count-sensitive (absolute slots 1..N*8); everything else (CommandMode VPOT idx 0-7, PlugMode slots/banks/pages, PlugMap offsets) is relative-within-8-block / FX-param-relative. Out-of-range anchor/q_channel = INACTIVE/hidden (never clamp-and-destroy), both on load AND at runtime (when active set shrinks via release feature). Add version="1" attr to XML root for migration. No per-unit project state for linked mode. Re-check per-mode when designing each mode for N units.
- 2026-07-07: WP-A (HardwareUnit) open questions ALL 6 RESOLVED (ai-docs/extender-wp-a-hardware-unit.md §9 now a decisions section): (1) Input indexing = GLOBAL — HardwareUnit knows m_unitIndex, adds offset, emits global channel 1..N*8; surface/modes never see local on input. (2) Master = logical channel 0 BROADCAST now — setFader(0)→all units' setMasterFader, any unit master move→fader(0); per-unit master deferred to independent-mode. (3) Hardware-state caches (faderPos[9], ledState, stateRec, ...) MOVE INTO HardwareUnit; CCSManager keeps only logical/mode state. (4) Display multiplexer = COMPOSITE MultiDisplay — mode holds a composite Display (IS-A Display) owning N real per-unit child Displays; changeField(row, globalField 1..N*8)→child[(f-1)/8].changeField(row,(f-1)%8+1); rows 2-3 (ProX) silently dropped on non-ProX units; per-unit DisplayHandler/Display stay byte-identical to today; modes change only field range + construction site (new Display(h,4)→createMultiDisplay(h,N)). Chose composite over CCSManager-router (keeps per-unit path untouched + modes own their display) and over modes-loop (avoids touching every mode's render now). (5) Multiple main units = ACCEPT redundancy (all fire, idempotent; no primary-main concept). (6) MIDI-open/JACK sequencing = open ALL N outputs, ONE 200ms usleep, reset all, start all inputs (200ms total, independent of N) — CAVEAT: PipeWire-JACK buffer race (MEMD 2026-06-27) only observed with 1 port; N-port variant UNVERIFIED, must test on real multi-unit hardware at WP-A integration; fallback = per-port 200ms (200ms×N).
- 2026-07-07: WP-A scope = N=1 extraction milestone ONLY (NOT an N-generic core). Hard numUnits()==1 invariant + ASSERT(globalChannel<=availableChannels()). The translation plumbing (unitForChannel/localOf/broadcastMasterFader/master-as-channel-0) is written N-generic, but N>1 CONSUMERS stay 1..8/global: CCSManager channel bounds+asserts, VPOT_LED[9], MeterBridge routing, ButtonManager per-note state, mode ProX-row logic. Enabling N>1 is a separate later milestone: WP-B (per-unit config) → WP-C (Tracks N*8) → WP-F (channel arrays + VPOT/meter routing + per-unit button state) → per-mode N-unit design → hardware test (CR-R3 JACK N-port caveat). WP-B alone does NOT enable N>1. Rejected alternative: front-loading WP-F work into WP-A (would ~double scope, no N=1 value gain). Legacy g_mcu_list/GetOffset/GetSize stay as shims through WP-A (used by MultiTrackMode + Tracks) — deleted only in WP-C/WP-F.
- 2026-07-12: CORRECTION (recurrent confusion, fixed for the 3rd time): unit position and main/extender role are ORTHOGONAL. There is NO "unit 1 must be main" rule — not in design, not in code. The dialog offers ALL five type options (Mackie Main/Extender, QCon ProX/Extender, Disabled) for EVERY unit position. A main unit can sit anywhere, including the rightmost slot. SurfaceConfigDialog.cpp's stale comment referencing a nonexistent `supportedUnitTypes` has been fixed; AGENTS.md's "Unit 1 type combo is main-only" claim has been fixed. Consequence: a config with ZERO main units IS possible (misconfiguration) → must be caught at configFunc time (warn/reject), NOT silently tolerated by a routing-helper fallback to unit 0. WP-EF plan Step 1b adds this validation. Do NOT re-introduce a "unit 0 is the main" assumption anywhere.
- 2026-07-12: WP-EF plan REVISED after critical review (ai-docs/extender-wp-ef-critical-review.md, 11 findings). All verified factual claims were correct. Key changes: (1) F2 SCOPE CORRECTION — first draft claimed to enable channels 9+, which was self-contradictory (modes not widened). Revised WP-EF delivers ONLY: routing correctness (capability-based, no unit-0 hardcoding) + global multi-main output (transport/timecode/assignment on every main unit). Channels 9+ DEFERRED to per-mode work. Input gate for strip events RETAINED. (2) WP-EF-0 added: dense-unit topology invariant (no gaps — active units contiguous from position 0, enforced at configFunc), because unitForChannel() indexes the compact m_units vector and gapped configs corrupt routing. (3) Concrete bugs fixed in plan: F3 (MeterBridge % 8 logic bug + strip/master state collision → split into sendStripMeter/sendMasterMetersToProXUnits), F4 (fader channel is in STATUS byte midi_message[0] low nibble, NOT data byte; listener path is dead code, never wired), F6 (SetLED cannot route strip LEDs — note encodes LOCAL channel not unit; setStripLED(globalChannel,...) is mandatory, SetLED becomes global-only with ASSERT), F8 (Transport::instance() doesn't exist — it's m_pTransport member; cache invalidation needed after reset since sendStripFader dedupes). (4) CONFIG_FLAG_PROX define RETAINED (8 mode files still use it; deleting breaks compile) — only routing-layer uses removed. (5) N=1 promise is protocol-state equivalence, NOT byte-identical (dedup changes wire traffic). Exit criteria: 11 points adopted from review. Net: WP-EF is smaller and honest — "routing correct + multi-main global output", NOT "N*8 channels". Merge still justified (shared SetLED/SendMidi/ProX sites + topology prereq).
- 2026-07-12: "At least one main unit" requirement REMOVED. Configs with zero main units are now explicitly allowed. When there are no main units, global output (transport LEDs, timecode, assignment) falls back to unit 0. No code validation was ever implemented for this; the requirement was only in comments/plans. Updated: SurfaceConfigDialog.cpp comment, AGENTS.md, ai-docs/extender-wp-ef-impl-plan.md, ai-docs/extender-wp-ef-critical-review.md, ai-docs/extender-wp-b-impl-plan.md.

## Active Context

- Upgrade reaper_plugin_functions.h from pinned v5.92 (vendor/) to reaper-sdk/sdk/ v7.74: delete the vendor/ copy, let the build resolve via ${SDK_DIR}. Separate deliberate step — it is a REAPER API version change with potential behavior impact.
- GPL migration: headers done (134 files, commit 4350ee2). Manual .tex files already reference GPLv3 — double-check German version. notes.org still lists "Update Licence to GPLv3" as TODO — likely stale but verify.
- PerformanceMode is an intentional stub — implement later.
- ReaPack packaging desired but not done.
- WP-A (HardwareUnit) DONE. WP-B (Multi-Unit Configuration) DONE. WP-C (Tracks N*8) DONE. WP-D (Banking Semantics) DONE.
- WP-E and WP-F merged into **WP-EF** (Full Hardware Routing). Rationale: WP-E had no standalone N>1 value; the two share SetLED/SendMidi call sites and ProX quirk sites. Combined plan at `ai-docs/extender-wp-ef-impl-plan.md`. Next implementation step.
- Fader startup bug (MEMD 2026-07-09) still open — needs separate investigation, NOT part of WP-EF.

## Bugs & Fixes

- 2026-07-05: `csurf_main.cpp` (line ~201): the macOS-port load-marker block (commit 4311da3) used `__attribute__((constructor))` + `/tmp/klinke_*.txt` writes. `__attribute__` is GCC/Clang-only — MSVC parses `constructor` as an undeclared identifier (errors C2065/C4430/C2062/C2143/C2447), blocking the Windows build. Wrapped the ctor block in `#if !defined(_WIN32)` (CRLF preserved via Python edit); macOS/Linux behaviour unchanged. The `/tmp/klinke_entry.txt` writes inside `REAPER_PLUGIN_ENTRYPOINT` are harmless no-ops on Windows (fopen fails, `if(f)` guards) and were left as-is. This scaffolding should eventually be cleaned up or gated behind `MCU_DEBUG_LOG`.
- 2026-07-06: DisplayHandler NULL pointer guard (2026-06-22, Rothchild fix #10): sendToHardware() dereferenced m_pActualDisplay without NULL check. When MIDI device init fails (no hardware / wrong device), m_pActualDisplay stays NULL → SIGSEGV on first PanMode::activate(). This was THE crash on "Add control surface → OK".
- 2026-07-06: a2jmidid LED bug (2026-06-23): a2jmidid converts Note On velocity 0 → Note Off (issue jackaudio/a2jmidid#14). MCU hardware ignores Note Off for LED control → LEDs never turn off and never blink. Fix: in SendMidi(), velocity 0x00 with Note-On status (0x90-0x9F) is mapped to 0x02; all direct m_midiout->Send(0x90,...) calls replaced with SendMidi(...); DropState::updateMCU() maps LED_OFF inline to 0x02. Affected: PROX devices with a2jmidid; the JACK-MIDI bridge is not affected.
- 2026-07-06: PipeWire-JACK startup crash (2026-06-27): SIGSEGV in process_empty, data-loop.0, pipewire-jack.c:1783. Triggered by our extension opening a JACK MIDI output port and sending MIDI before PipeWire finishes buffer allocation. PERMANENT FIX: Sleep(200ms) in CSurf_MCU constructor, after CreateMIDIOutput, before MCUReset(). linux=usleep(200000), win32=Sleep(). csurf_mcu.cpp.
- 2026-07-06: macOS configFunc crash (2026-07-04): SIGSEGV at 0x0. Root cause: swell-modstub-generic.cpp bound SWELL via SWELL_dllMain getfunc param, which REAPER passes as NULL on macOS. All SWELL function pointers remained NULL → DialogBox/CreateWindow dereferenced NULL. Fix: switched macOS build to swell-modstub.mm, which binds SWELL at dylib load via REAPER's NSApp delegate ([del swellGetAPPAPIFunc]).
- 2026-07-06: macOS case-insensitive APFS (2026-07-03): root-level project files collided with system headers — VERSION→<version> (libc++ parse error), Assert.h→<assert.h> (assert undeclared cascade). Fix: rename VERSION→VERSION.txt, Assert.h→McuAssert.h. The clang warning "non-portable path to file '<X>'; specified path differs in case" is the smoking gun. Watch for new root files.
- 2026-07-08: Faders 1-8 not moving to correct position on startup after WP-A Step 4. Root cause: HardwareUnit::m_faderPos initialized to 0, but original CCSManager used -1 as sentinel. With m_faderPos=0, the dedup check `m_faderPos[local] != value` prevented any fader whose initial value was 0 from being sent (fader at bottom). Master fader unaffected because its non-zero initial value passed the dedup. Fix: initialize m_faderPos to -1 in HardwareUnit ctor (matching original behavior).
- 2026-07-08: Faders sometimes don't move to correct position on startup or after closing the surface-edit dialog. Needs investigation — likely a fader-cache invalidation issue during surface (re)initialization.
- 2026-07-09: Faders staying at zero on startup / after surface dialog close — ROOT CAUSE FOUND AND FIXED. When Reaper calls SetSurfaceVolume for the first track, it triggers updateFaders() which iterates ALL channels. Tracks not yet registered in m_surface_volume returned 0.0 from GetSurfaceVolume() → volToInt14(0.0)=0 → all other faders sent to 0. The master fader didn't have this problem because it reads directly from Reaper. Fix: GetSurfaceVolume and GetSurfacePan now fall back to reading directly from Reaper (GetSetMediaTrackInfo) when the track isn't in the cache, matching the master fader approach. This is self-healing: once SetSurfaceVolume is called, the cache takes over.
- 2026-07-09: Faders going to zero on startup — second fix layer. Root cause: m_channelTracks[1..8] was all NULL during the first updateFaders() because the track graph hadn't been built yet (buildGraph is called from tracksStatesChanged, which runs every 3rd Run() call). The NULL-check in updateFaders explicitly sends 0 for NULL channels. Fix: (a) GetSurfaceVolume/GetSurfacePan now read directly from Reaper when cache misses (self-healing); (b) tracksStatesChanged(false) called in CSurf_MCU constructor to build initial graph before SetSurfaceVolume arrives; (c) added MCU_LOG diagnostics to updateFaders and GetSurfaceVolume to track the startup sequence. Debug log: ~/.config/REAPER/mcu_klinke_debug.log
- 2026-07-09: Fader-zero-on-startup: pre-existing bug from WP-A. Debug log evidence (2026-07-09): first updateFaders() runs with totalTracks=0 → all 8 channels NULL → sends 0 to hardware. Later GetSurfaceVolume cache misses read correct volumes from Reaper (confirmed via log), but faders stay at 0. Attempted fixes that DID NOT work: (a) GetSurfaceVolume/GetSurfacePan Reaper fallback on cache miss — correct volumes were read but faders still at 0; (b) tracksStatesChanged(false) in constructor — project not loaded yet, useless; (c) HardwareUnit m_faderPos=-1 sentinel — dedup passes correctly. The real problem is downstream of GetSurfaceVolume: either the correct MIDI doesn't reach the hardware, or something sends 0 AFTER the correct values. Other symptom: track selection also not recognized at startup (likely same root cause — surface not syncing with Reaper). Suspects: thread race between Run()/audio-thread and SetSurfaceVolume/main-thread, or MIDI output not flushing, or PipeWire/JACK buffer issue specific to startup. Needs separate systematic investigation.
- 2026-07-12: Fader-zero-on-startup + solo-LED-not-correct-on-startup FIXED by WP-EF Step 7 (cache invalidation + reset order). Root cause: HardwareUnit caches (m_faderPos[0..8], m_led_state[128]) were stale after hardware reset (SysEx). MCUReset() published LEDs → forceAllLEDsOff() cleared them → only Transport::updateLeds() re-published (B_MARKER/B_NUDGE). Drop/Zoom/Scrub/Assignment LEDs stayed off. Faders: sendStripFader(0) was deduped because cache already held 0 from ctor init. Fix: (a) invalidateFaderCache() sets m_faderPos to -1 sentinel; invalidateLEDCache() sets m_led_state to LED_UNKNOWN; (b) constructor order: reset → invalidate → MCUReset (state only) → forceAllLEDsOff (actually sends) → re-publish ALL surface LEDs + Transport. (c) destructor also invalidates before per-unit shutdown.

## Changelog

- 2026-07-05: Added `scripts/build-windows.sh`: builds `reaper_csurf_mcu_klinke_x64.dll` from WSL using the native MSVC toolchain (VS2019) and auto-deploys it to `%APPDATA%\REAPER\UserPlugins\`. Verified: PE32+ x86-64 DLL, exports `ReaperPluginEntry`, clean build ~1m30s, incremental ~1m14s. Documented in AGENTS.md §4 ("Building from WSL"). `.gitignore` now ignores `build_win/`.
- 2026-07-06: Directory restructure plan v3 executed (2026-07-06). Sources moved into src/ tree; no .cpp/.h remain in repo root. Tree layout: src/{core,state,display,ui,modes/{commands,multitrack,sends,plugin{,/editor}}}. Vendored headers → vendor/. Resources → resources/. All scripts → scripts/. Dead VS files → archive/vs-legacy/. JUCE-changes → archive/juce-1.52-patches/. Stale debug log removed. Linux build verified working. Include strategy: explicit subdir list (GLOB_RECURSE had ARG_MAX issues). 3 include files edited (res_linux.cpp + csurf_main.cpp: repo-root-relative paths → flat includes using SWELL_DIR/WDL_DIR). res.rc_mac_menu added to git (generated empty file, now in resources/).
- 2026-07-06: Restructure refinements (2026-07-06): (a) Selector → hardware/display/ (belongs to display subsystem). (b) state/ dissolved, all files → core/ (boundaries too fuzzy). (c) src/action/ created: Actions, ActionsDisplay, ActionsDialogComponent extracted from modes/commands/ (maintainer: Actions ≠ CommandMode). (d) src/hardware/ created with display/ subdir: mcu_button_defines.h, ButtonManager, VPOT_LED → hardware/; Display, DisplayHandler, MeterBridge, Selector → hardware/display/. src/display/ dissolved. Build verified.
- 2026-07-06: Removed `posix_shims/windows.h` (dead code). Every `#include <windows.h>` in the sources is either `#ifdef _WIN32`-gated (never reached on Linux/macOS) or already uses the explicit `#ifdef _WIN32 … #else #include "swell.h" #endif` pattern (Actions.cpp). No TU ever reached the shim, so the `-Iposix_shims` include path (CMakeLists.txt) and the directory were deleted. No source file changed. Do NOT re-add the shim — if a source genuinely needs SWELL on non-Windows, use the Actions.cpp `#ifdef _WIN32/#else #include "swell.h"#endif` pattern directly.
- 2026-07-06: Updated all source/header license headers from GPLv2 to GPLv3 (134 files). Copyright updated to 2009-2026 Steffen Fuerst. Cockos files (csurf_main.cpp, csurf_mcu.cpp) left with original LGPL. Files without headers got GPLv3 header prepended. PlugAccess.cpp had a UTF-8 BOM stripped during the update.
- 2026-07-06: macOS Clang build: all warnings eliminated (was 14 unique warning types across 11 files). Fix categories: (1) -Wswitch: added missing enum cases/breaks in 8 files (Transport, Tracks, VPOT_LED, DisplayHandler, PlugAccess, PlugModeSelectors, PlugMode, SendReceiveModeBase). (2) -Wparentheses: added explicit parens around assignment-as-condition in Tracks, MultiTrackMeterBridge, PlugMode. (3) -Wformat-security: ProjectConfig.cpp AddLine calls with non-literal strings now use "%s" format. (4) -Wdelete-abstract-non-virtual-dtor: MeterBridge.h got virtual ~MeterBridge(). (5) -Wstring-plus-int: csurf_mcu.cpp changed "str" + pos to &"str"[pos]. (6) -Wdangling-else: Transport.cpp added braces. (7) -Wbitwise-conditional-parentheses: csurf_mcu.cpp parenthesized | expression before ?:. (8) -Wmismatched-new-delete: Display.cpp safe_delete → safe_delete_array for new[] allocation. Build: zero warnings, clean compile on Apple Clang.
- 2026-07-07: WP-A implementation plan created via planner subagent → ai-docs/extender-wp-a-impl-plan.md (524 lines). 8 ordered, incrementally-compilable steps, each with a "keep N=1 working" checkpoint: (1) add new types as unused stubs; (2) HardwareUnit owns MIDI I/O + reset + raw send, CSurf_MCU forwards; (3) per-unit DisplayHandler + per-unit isProX; (4) per-strip LED state + fader cache into HardwareUnit, SetLED becomes strip/global router; (5) input parsing into HardwareUnit emitting global events; (6) global↔local translation + master broadcast; (7) MultiDisplay composite + factory, rewire mode construction; (8) dead-scaffolding removal (g_mcu_list, m_is_mcuex, FIXID, csurf_mcuex_modified_reg). Golden thread: CSurf_MCU keeps SetLED/SendMidi/SendMsg/GetDisplayHandler as forwarding shims so existing callers don't change until late steps. WP-A ships N hardcoded to 1 (legacy 5-int string → 1 main UnitConfig); internal structure is N-generic for WP-B. 8 risks logged (R1 MultiDisplay vs single-active-display switchTo is highest).
- 2026-07-07: WP-A implementation plan (ai-docs/extender-wp-a-impl-plan.md) revised per critical review (ai-docs/extender-wp-a-critical-review.md, 9 findings, ALL valid). Headline changes: (1) F1: N-generic promise → honest N=1 extraction milestone with hard numUnits()==1 invariant + ASSERT(globalChannel<=availableChannels()); N>1 enablement explicitly requires WP-C+WP-F+per-mode, NOT just WP-B. (2) F2: legacy g_mcu_list/GetOffset/GetSize retained as SHIMS through WP-A (still used externally: MultiTrackMode.cpp:217,230,241-245 + Tracks.cpp:625) — deleting now breaks compilation; only FIXID/EXT_ID/csurf_mcuex_modified_reg deleted in WP-A. (3) F3: DisplayHandler send path made PER-UNIT (m_pUnit->sendMsg via back-pointer/HardwareUnit*), NOT via CSurf_MCU::SendMsg shim (which routes to m_units[0] and would send all per-unit displays to unit 0). (4) F4: OnRotaryEncoderPush (note range 0x20-0x27) moved to PER-UNIT strip path — was misclassified global; VPOT-push is per-strip like rec/solo/mute/select. (5) F6: explicit display-switching service on CSurf_MCU + COMPLETE switchTo callsite list (added the ones the plan missed: splash csurf_mcu.cpp:287, action-display :680, Options.cpp:45, ActionsDisplay.cpp:27/33); switchTo(MultiDisplay) documented INVALID (sendDifferences pDisplay==m_pActualDisplay check). (6) F5/F7/F8: ButtonManager per-unit state, ProX-in-modes (16 CONFIG_FLAG_PROX sites), VPOT/meter routing all explicitly DEFERRED (WP-F + per-mode); mixed main/extender models OUT OF SCOPE until then. (7) F9: build/verify snippet copy path fixed (subshell around `cd build`). Maintainer agreed: honest N=1 boundary, NOT front-loading WP-F (would ~double WP-A scope without raising N=1 value).
- 2026-07-08: WP-A Step 2 (HardwareUnit owns MIDI I/O + reset + raw send) DONE. Used a LEANER CACHED-POINTER APPROACH instead of full m_midiout/m_midiin removal: m_midiout and m_midiin stay as NON-OWNING cached pointers into m_units[0]'s ports. This keeps all legacy call sites (MCUReset, Run, SetPlayState, OnZoom, IsModifierPressed, ...) unchanged — zero call-site churn. Ownership lives in HardwareUnit dtor (DELETE_ASYNC on close). Rationale: token-efficiency + lower risk for N=1 behavioral identity. These cached pointers will be removed in a later step once all ~40 sites route through the unit explicitly. The HardwareUnit.h interface and MultiDisplay stubs are in place from Step 1; Steps 3-8 build on this post-restart.
- 2026-07-08: WP-A ALL 8 STEPS COMPLETE. HardwareUnit abstraction extracted from CSurf_MCU with hard N=1 invariant. Summary: (1) HardwareUnit/UnitConfig/DeviceModel/MultiDisplay/listener stubs added. (2) MIDI I/O + reset SysEx + raw send in unit; m_midiout/m_midiin kept as NON-OWNING cached pointers for zero call-site churn. (3) DisplayHandler per-unit: HardwareUnit* backpointer, per-unit isProX, per-unit send path via m_pUnit->sendMsg. (4) LED state m_led_state[128] + fader cache m_faderPos[9] moved to unit; CSurf_MCU::SetLED routes to unit; CCSManager dedup arrays (m_stateRec/Solo/Mute/Select, m_faderPos) removed. (5) Run() MIDI read iterates m_units; OnMIDIEvent dispatch stays on CSurf_MCU (parser split deferred). (6) Translation helpers: numUnits, unitForChannel, localOf, broadcastMasterFader, availableChannels; Tracks::adjust now uses availableChannels(). (7) MultiDisplay class fully implemented; CCSManager::createDisplay() factory; all 9 mode new Display() → factory. (8) Dead scaffolding removed: FIXID, EXT_ID, csurf_mcuex_modified_reg, single-instance MessageBox, commented mcuex extern/Register. Remaining shims: g_mcu_list, m_is_mcuex/m_offset/m_size, IsExtender/GetOffset/GetSize, cached m_midiout/m_midiin (deferred to WP-C/WP-F).
- 2026-07-08: WP-B Complete Multi-Unit Configuration implemented (2026-07-08): SurfaceConfig data model with parse/serialize for legacy 5-int and new KLINKE2 8-entry format. createFunc wired to SurfaceConfig; ctor constructs HardwareUnits for all configured units with duplicate-MIDI-device warnings. GetConfigString emits KLINKE2. GetDescString shows [N units] when N>1. Full 8-unit dialog with static resources (res.rc + res.rc_mac_dlg widened to 350x310). IDC_PROX hidden; PROX now per-unit via device type combo. Input gate drops events from units 1+ until WP-C+WP-F.
- 2026-07-09: WP-C (Tracks Singleton Refactor to N*8) COMPLETE. 9 steps implemented per revised plan (reordered per critical review). Key changes: (1) Tracks gains m_numMCUChannels member (default 8, clamped 8-64). (2) getNumberOfChannelStrips() returns runtime count instead of hardcoded 8. (3) adjust() calls createChannelTrackVector() on count change (F1 fix from critical review). (4) m_channelTracks dynamically resized to m_numMCUChannels+1. (5) getMediaTrackForChannel() works for channels above 8. (6) getChannelForMediaTrack() uses comparison (==) not assignment (typo fix) and loops over full dynamic range. (7) findMediaTrackForChannel() adds early out-of-range bailout, removes m_pMCU->GetOffset(), filters anchors by active range only. (8) getNumberOfActiveAnchors() added — counts only anchors in 1..numMCUChannels (maxChannel parameter, default m_numMCUChannels). (9) moveSelectedTrack2MCU() uses active anchors + m_numMCUChannels, early return before setGlobalOffset(0) if numChannels<=0. (10) moveTrackToLeftMostChannel() anchor-counting loop bounds j<=m_numMCUChannels and checks anchorChannel<=m_numMCUChannels. (11) updateTrackStates() guards numMCUChannels<=0. (12) MultiTrackMode GetSize()→getNumberOfChannelStrips() (deferred g_mcu_list loop to WP-D). (13) dumpMappingState() debug helper added (MCU_LOG-gated). Build: clean (zero errors/warnings). Deployed to ~/.config/REAPER/UserPlugins/.
- 2026-07-09: WP-D (MultiTrack Banking Semantics) COMPLETED. Tracks now owns all banking math via 5 new helpers (getEffectiveBankStep, getLegacyPageStep, getMaxUsefulGlobalOffset, clampGlobalOffset, clampCurrentGlobalOffset). setGlobalOffset returns bool and clamps internally. MultiTrackMode::frameUpdate() uses clampCurrentGlobalOffset() instead of hard-coded msize-8. buttonFaderBanks() uses helper steps, no longer scans g_mcu_list or calls getNumberOfAnchors(). Tracks::adjust() clamps offset after channel-count change. MultiTrack display/control loops remain capped at 1..8 with WP-F widen comments. Build: zero warnings, clean compile.
- 2026-07-09: Reverted all fader-init "fixes" from WP-D session — they didn't solve the root cause. GetSurfaceVolume/GetSurfacePan Reaper fallback reverted, HardwareUnit debug logging reverted, constructor tracksStatesChanged reverted. The fader-zero-on-startup bug is pre-existing (likely from WP-A) and needs separate root-cause analysis. Also affecting: track selection not recognized on startup.
- 2026-07-12: WP-E and WP-F merged into WP-EF (Full Hardware Routing). Rationale: WP-E (global routing) had no standalone N>1 value — strip LEDs stayed on unit 0, creating an untestable intermediate. Both shared SetLED()/SendMidi() call sites and CONFIG_FLAG_PROX/ProX quirk sites. Combined into 14-step plan: Steps 1-6 (global routing: helpers, SetLED classifier, direct send replacement, assignment display, DropState, blink emulation), Steps 7-13 (strip widening: VPOT_LED sizing, strip LED channel routing, MeterBridge, fader routing, ButtonManager per-unit state, input gate removal, startup/reset coherence, legacy shim removal). Step 14 = verification. Plan: ai-docs/extender-wp-ef-impl-plan.md. Master plan (extender-support.md) updated with merged WP-EF section and implementation summary table (§9).
- 2026-07-12: WP-EF (Full Hardware Routing) implemented per ai-docs/extender-wp-ef-impl-plan.md. Linux build: zero warnings, clean. Key changes:
- 2026-07-12: WP-EF-0: Dense unit topology enforced (dialog reject, createFunc fallback, ctor assert). hasDenseUnitTopology + unitIsDisabled exported from SurfaceConfig.
- 2026-07-12: Step 1: 13 new routing helpers in csurf_mcu.h/.cpp (isTransportUnit, hasTransportUnits, setGlobalLED, sendMidiToTransportUnits, sendMidiToAllUnits, setStripLED, sendStripCC, sendStripFaderToUnit, sendStripMeter, sendMasterMetersToProXUnits, isGlobalLedNote, isStripLedNote, anyUnitNeedsBlinkEmulation).
- 2026-07-12: Step 2: SetLED global-only (ASSERT isGlobalLedNote). All global output sites migrated: MCUReset, SetPlayState, SetRepeatState, UpdateAutoModes, ClearSaveLed, OnSave, ClearUndoLed, OnUndo, OnZoom, OnScrub, Run() SMPTE/beats/time digits. !m_is_mcuex output gates removed. m_globalInputUnitIndex computed.
- 2026-07-12: Step 2c: setAssignmentDisplay routes to all transport-capable non-ProX units.
- 2026-07-12: Step 3: DropState → pure state holder (toggleState/updateReaper/ledState, removed toggleStateAndUpdate/updateMCU). OnDropButton calls toggleState+updateReaper+SetLED.
- 2026-07-12: Step 4: setRecLED/setSoloLED/setMuteLED/setSelectLED → setStripLED(globalChannel, localNote, state).
- 2026-07-12: Step 4a: Destructor per-unit LED/fader shutdown with cache invalidation.
- 2026-07-12: Step 5: VPOT_LED gets per-unit m_isProX flag; updateLEDs uses sendStripCC (owning unit). CONFIG_FLAG_PROX removed from VPOT layer.
- 2026-07-12: Step 6: MeterBridge state split: m_stripMeterPos (dynamic vector) + m_masterMeterPos[2] (fixed). Two send paths: sendStripMeter (0xD0, owning unit) + sendMasterMetersToProXUnits (0xD1, all ProX). All 3 MeterBridge subclasses updated.
- 2026-07-12: Step 7: HardwareUnit::invalidateFaderCache/invalidateLEDCache. Constructor reset order: reset → invalidate → MCUReset → forceAllLEDsOff → publish initial state.
- 2026-07-12: B_PAUSE, L_SMPTE, L_BEATS added to mcu_button_defines.h.
- 2026-07-12: Verification passes: zero m_midiout->Send (except SendMidi shims), zero m_is_mcuex output gates, zero CONFIG_FLAG_PROX in routing layer.

## Patterns

- User can run sudo commands when requested — just tell them what to run and they'll execute it. No sudo password prompt needed from agent side.
- Version source = VERSION.txt file (repo root), NOT hard-coded. Format "<version> <build-count>". Bump version manually (reset count); count auto-increments per `cmake` configure. Generated build/Version.h → MCU_VERSION_STRING in csurf_mcu.h GetDescString. AGENTS.md §4 documents it.
- Deploy is the AGENT's job, not CMake's: after a successful Linux build, `cp build/reaper_csurf_mcu_klinke.so ~/.config/REAPER/UserPlugins/`. No CMake auto-deploy (keeps CI/Windows/macOS clean). Reaper needs full restart to reload.
- Logging architecture: MCU_DEBUG_LOG = standalone CMake OPTION (default ON), NOT tied to CMAKE_BUILD_TYPE → currently EVERY build logs, including Release. When OFF, MCU_LOG(...) compiles to ((void)0) (see src/core/McuDebugLog.h #else) → zero runtime cost. 16 call sites in 6 files; most verbose: sendToHardware logs every text write (ROW0/ROW1 snd). Release goal: default OFF, ON only for debug builds.
- Build-flow rule (always run both steps, otherwise no build-count increment): `cd build && cmake .. -DCMAKE_BUILD_TYPE=Release && cmake --build . -- -j$(nproc)`. The increment happens ONLY in the configure step (`cmake ..`), NOT in the build step. Running only `cmake --build` (incremental) without `cmake ..` first produces a build without an incremented counter. Mnemonic: "configure = increments, build = links".
- Commit messages always in English (user requirement).
- No commits without explicit user instruction (user requirement).
- Always write links out in full (https://...), not as Markdown hyperlinks [text](URL) — the user cannot click Markdown links.
- HIGH-PRIORITY LANGUAGE RULE: the dialog with the user may be in German, but EVERYTHING written into a file (source, comments, strings, logs, commit messages, docs, AGENTS.md, manual, MEMD.md, generated code, configs, scripts) MUST be in English. Never write German text into a file, not even comments. Anchored at the very top of AGENTS.md as "⚠️ Language rule (HIGH PRIORITY)".
- macOS case-insensitivity hazard: project files at repo root (on the -I path) collide case-insensitively with system headers on APFS. Found: VERSION→<version> (libc++), Assert.h→<assert.h>. The clang -Wnonportable-include-path warning ("differs in case from file name on disk") is the tell. Mitigation: unique file names (VERSION.txt, McuAssert.h). Watch for new ones whenever a new root-level header is added.
- Modern Boost needed for libc++/C++17: keep Boost ≥ 1.81 (we pin 1.91.0). Old Boost (1.39) requires heavy patching for modern compilers; not worth it.
- macOS build: NEVER set CC to Homebrew gcc — JUCE 8 juceaide needs Apple Clang. CMakeLists.txt forces Clang on Darwin. Override with -DCMAKE_C_COMPILER if ever needed.
- macOS JUCE event dispatch is automatic via NSRunLoop (driven by REAPER main thread); do NOT call dispatchNextMessageOnSystemQueue on macOS (no impl in JUCE 8). Linux/X11 needs the manual pump (#if JUCE_LINUX in csurf_mcu.cpp Run()).
- macOS SWELL stub: each platform has a DIFFERENT swell-modstub variant. macOS = swell-modstub.mm (NSApp delegate constructor), Linux = swell-modstub-generic.cpp (SWELL_LOAD_SWELL_DYLIB dlopens libSwell.so, OR non-dlopen path with SWELL_dllMain getfunc), Windows = none (native Win32). Never use the generic stub on macOS; REAPER does not pass the getfunc through SWELL_dllMain there.
- Portable Linux build gotchas (docker/release-linux.Dockerfile, all container-scoped): (1) JUCE 8 needs cmake ≥3.22 but Debian 11 ships 3.18 → install the cmake.org binary tarball. (2) juceaide (JUCE's codegen helper) is built by a RECURSIVE cmake subprocess whose PASSTHROUGH_ARGS does NOT include CMAKE_C[XX]_FLAGS — so forcing -I/usr/include/freetype2 via CMAKE_CXX_FLAGS does NOT reach juceaide. (3) juceaide compiles juce_graphics.cpp which needs <ft2build.h> (flat header under /usr/include/freetype2/) and <fontconfig/fontconfig.h>. Fix: symlink /usr/include/ft2build.h → freetype2/ft2build.h AND /usr/include/freetype → freetype2/freetype. Plus install libfontconfig1-dev. (4) Debian gcc enables -Wl,--as-needed by default → drops unused -l libs from NEEDED (X11 gets dropped; harmless — JUCE loads X11 via dlopen). (5) Need libxcursor/xinerama/xrandr/xrender/xcomposite-dev + libglu1-mesa-dev for JUCE pkg-config module deps. (6) Static libstdc++.a on Debian 11 is built with -fPIC, so -static-libstdc++ works for a SHARED lib.
- Subagent `output` path quirk: when a foreground subagent is given a relative `output` path (e.g. "ai-docs/foo.md"), the framework does NOT write it into the repo at that path — it nests it under `.pi-subagents/artifacts/outputs/<runId>/ai-docs/foo.md` instead (plus a copy at `.pi-subagents/artifacts/<runId>_<agent>_<index>_output.md`). If the artifact must live in the repo, `cp` it from the artifacts dir afterwards. Confirmed 2026-07-07 with the planner run.
- Subagent "needs attention (no observed activity for 60s)" notifications can be STALE: they race against run completion. A run that already returned its output inline is DONE (verify with `subagent status` → look for "exit 0 / completed") — do NOT resume/interrupt a finished run on this signal alone.
