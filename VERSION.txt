# Format: <version> <build-count>
#
# version       bump MANUALLY for a release (e.g. 0.9.1.3 -> 0.9.2.0).
#               When you bump the version, reset build-count to 1.
# build-count   edited MANUALLY only. The number stored here is used verbatim
#               and compiled into the binary as "... build N".
#
# This file is only ever READ by CMake; it is never rewritten automatically.
# Edit it by hand to change the version or build number, then configure.
# Displayed in Reaper as: v<version> build <count>.
0.9.7.0 9
